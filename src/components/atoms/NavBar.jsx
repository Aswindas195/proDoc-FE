import { Box } from "@mui/material";

const NavBar = () => {
  return (
    <Box
      sx={{ width: "100vw", height: "100px", backgroundColor: "#007bff" }}
    ></Box>
  );
};

export default NavBar;
